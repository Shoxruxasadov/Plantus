# Инструкция по деплою в TestFlight

## Проблема решена! ✅

Теперь build number автоматически увеличивается перед каждой сборкой.

## Как деплоить в TestFlight:

### Простой способ (рекомендуется):
```bash
npm run deploy:testflight
```

Эта команда автоматически:
1. ✅ Увеличит build number
2. ✅ Соберет iOS приложение
3. ✅ Отправит в TestFlight

### Ручной способ (если нужно собрать и отправить отдельно):

```bash
# 1. Собрать билд (build number увеличится автоматически)
npm run build:ios

# 2. Отправить в TestFlight
npm run submit:ios
```

## Как это работает:

- Скрипт `scripts/increment-build.js` автоматически увеличивает `buildNumber` в `app.json` перед каждой сборкой
- Это гарантирует, что каждый билд имеет уникальный номер
- Больше не нужно вручную помнить об увеличении build number!

## Текущие значения:

- **Версия:** `2.0.0` (в `app.json` → `version`)
- **Build Number:** Автоматически увеличивается (сейчас: `11`)

## Важно помнить:

1. ✅ Build number увеличивается автоматически - ничего делать не нужно
2. ✅ Версию (`version`) нужно менять вручную только когда хотите выпустить новую версию приложения
3. ✅ Для TestFlight достаточно увеличивать build number (это делается автоматически)

## Если возникнут проблемы:

1. Убедитесь, что используете последнюю версию eas-cli (скрипты используют `npx eas-cli@latest`)
2. Проверьте, что вы авторизованы: `eas login`
3. Убедитесь, что у вас настроены credentials: `eas credentials`

---

**Готово к деплою! Просто запустите `npm run deploy:testflight`** 🚀
